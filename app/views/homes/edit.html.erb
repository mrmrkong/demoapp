<h2>Edit</h2>

<div id="rspec-test" class="container">	
<ul class="nav nav-tabs" id="switch">
			<li class="active">
        		<a href="#1" data-toggle="tab">Tab 1</a>
			</li>
			<li>
				<a href="#2" data-toggle="tab">Tab 2</a>
			</li>
		</ul>

			<div class="tab-content">
			  <div class="tab-pane active" id="1">
			Tab1	  		
		  <hr></hr>
		  <%= simple_form_for(@home, html: { id:"unofficial-form" }) do |f| %>
		  	<%= f.input :name %>
		  	<%= f.input :size %>
		  	<%= f.input :owner %>
		  	<%= f.input :location %>
		  	<%= f.input :note %>
		  	<%= f.submit %>
		  <% end %>
		  <hr></hr>
		  <button class="btn btn-primary" id="copy-btn1to2">Copy to Tab2</button>
				</div>
				<div class="tab-pane" id="2">
					Tab2
					<hr></hr>
					<%= simple_form_for(@home, html: { id:"official-form" }) do |f| %>
						<%= f.input :name %>
						<%= f.input :size %>
						<%= f.input :owner %>
						<%= f.input :location %>
						<%= f.input :note %>
						<%= f.submit %>
					<% end %>
					<hr></hr>
					<button class="btn btn-primary" id="copy-btn2to1">Copy to Tab1</button>
				</div>
			</div>
</div>